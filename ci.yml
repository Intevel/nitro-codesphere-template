prepare:
  steps:
    - name: install
      command: npm install
    - name: build:types
      command: nitropack prepare
    - name: build
      command: nitropack build
test:
  steps: []
run:
  steps:
    - name: deploy
      command: npm run preview
